<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>留言板</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

</head>
<body>
  <h2> 留言板</h2>
  <div class="mb-2"> Hello <%= name %></div>

  <!-- 留言: -->
  <div class="">
    <% for(let i =0; i<data.length; i++) { %>
      <span class="me-2"><%= data[i].name %></span>
      <span class="me-2"><%= data[i].time %></span>:
      <span><%= data[i].message %></span>
      <hr/>
    <% } %>
  </div>
  <form action="/doSubmitMessage" method="post">
    <textarea name="message" placeholder="請輸入你的留言"></textarea>
    <button id="messageSubmit" type="submit">送出留言</button>
  </form>


  <!-- <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.27.2/axios.min.js"></script>
  <script>
    //測試 ejs 是否可以直接使用 script
    console.log(axios)
    axios.get('url/users?ID=123')
    .then((res) => { console.table(res.data) })
    console.log(messageSubmit)
  </script> -->
</body>
</html>